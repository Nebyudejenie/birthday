node_modules
.next
.git
.gitignore
.dockerignore
Dockerfile
docker-compose*.yml
npm-debug.log
*.md

# local env / secrets
.env
.env.*
!.env.example

# deployment infra (not part of the app image).
# Ignore only the shell scripts — prepare-standalone.mjs is needed by the
# `postbuild` step that runs during `npm run build` inside the image.
scripts/*.sh
nginx
cloudflared
logs
backup
ssl
